import '../models/server_config.dart';

const publicAiServerUrl = 'https://chat.publicai.co';

const publicAiDefaultServerConfig = ServerConfig(
  id: 'public-ai-default',
  name: 'Public AI',
  url: publicAiServerUrl,
  isActive: true,
);
